# Ignore windows terminal settings on all platforms. It will be symlinked with the script bootstrap.ps1
.config/windows-terminal

Readme.md

# Ignore files, which not needed on linux and macos
{{ if ne .chezmoi.os "windows" }}
.config/powershell
{{ end }}

# Ignore files, which not needed on windows
{{ if eq .chezmoi.os "windows" }}
.zshrc
.profile.dev
.chezmoi.toml
{{ end }}
