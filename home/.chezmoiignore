{{ if ne .chezmoi.os "windows" }}
# Ignore Windows-only configs on Linux and macOS
.config/powershell
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# Ignore Linux and macOS-only configs on Windows
.zshrc
.profile.dev
{{ end }}

# Ignore windows terminal settings on all platforms. It will be symlinked with the script bootstrap.ps1
.config/windows-terminal

{{/* Environment-based exclusions
     .env is set by bootstrap.sh via DOTFILES_ENV (devcontainer | dev_computer | home_lab)
     Use these blocks to exclude files that don't apply to a given environment.
     Example:
{{ if eq .env "devcontainer" }}
# Exclude desktop/GUI tools in devcontainers
.config/terminator
{{ end }}
*/}}
